/* import {GUI} from 'dat.gui'
import * as THREE from 'three'
//import { MainCamera } from '../assets'

const gui = new GUI()
const cameraFolder = gui.addFolder('Camera')
cameraFolder.add(MainCamera.position, 'z', 0, 10)

cameraFolder.open()

export { gui as GUIConfig} */